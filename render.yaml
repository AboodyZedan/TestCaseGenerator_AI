services:
  - type: web
    name: test-case-generator
    env: python
    buildCommand: ""
    startCommand: python app.py
    envVars:
      - key: GROQ_API_KEY
        value: ${GROQ_API_KEY}
